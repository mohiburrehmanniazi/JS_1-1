const prompt = require("prompt-sync")();
let result;
var name = prompt ("Enter Your Name")
var age = prompt ("Enter Your Age")
if( age >17) {
    console.log(name + " “Welcome, you are an adult”")}
    else{console.log(name + "“Sorry, you are not yet an adult”")}
